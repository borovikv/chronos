<%= form_for(@user, html: {class: 'form-horizontal'} ) do |field| %>

    <% if @user.errors.any? %>
        <div id="error_explanation" >
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= render('field', field: field, name: :email, type: :email) %>
    <%= render('field', field: field, name: :password, type: :password) %>
    <%= render('field', field: field, name: :password_confirmation, type: :password) %>

    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= field.button type: 'submit', class: 'btn btn-default' %>
      </div>
    </div>
<% end %>
