<p id="notice"><%= notice %></p>

<div id="board">
  <h1 class="board-name"><%= link_to @board.name, edit_board_path(@board) %></h1>

  <div>
    <%= link_to new_group_path board_id: @board.id do %>
        <span class="glyphicon glyphicon-plus"></span> New Group
    <% end %>
  </div>

  <%= render partial: 'groups/group', collection: @board.groups  %>

  <%= render 'cards/dialog' %>
</div>
