<%= render 'permission/form' %>
<div class="col-md-8">
  <% if @board.users.any? %>
        available to:
      <ul class="list-group">
        <%= render partial: 'user', collection: @board.permissions, as: 'permission', locals: {board: @board} %>
      </ul>
  <% end %>
</div>